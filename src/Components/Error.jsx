import { Typography } from "@mui/material"

function Error() {
    return (
        <Typography variant="h4" color="error.light" sx={{ display: "flex", justifyContent: "center", marginTop: "50px" }}>
            Nothing Found
        </Typography>
    )
}

export default Error